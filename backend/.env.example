# Server Configuration
PORT=3000

# Database Configuration (Option 1: Component fields)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=postgres
DB_SSL=false

# Database Configuration (Option 2: Connection String / URI)
# DATABASE_URL=postgresql://postgres:your_password@localhost:5432/postgres
